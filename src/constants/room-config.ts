export const ROOM_GRID_COLUMNS = 8;
export const ROOM_GRID_ROWS = 8;

// Coordinates are expressed in the 768 × 960 design canvas used by the room art.
export const ROOM_DESIGN_WIDTH = 768;
export const ROOM_DESIGN_HEIGHT = 1024;
export const ROOM_TILE_WIDTH = 88;
export const ROOM_TILE_HEIGHT = 64;
export const ROOM_ORIGIN_X = 384;
export const ROOM_ORIGIN_Y = 350;

// Coordinate labels and occupied-cell diagnostics are private-build helpers.
export const DEBUG_ROOM_GRID = true;
